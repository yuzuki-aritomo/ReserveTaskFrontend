version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/usr/src/app 
      - node-modules:/usr/src/app/node_modules
    command: sh -c "yarn dev"
    ports:
      - "3000:3000"